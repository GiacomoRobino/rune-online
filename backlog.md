# this backlog must not be implemented unless specifically prompted

- when summoned with multiple h/e, HE (card n 7), one rune is assigned to each copy instead of being assigned all to the original summoning, while the copies are tokens with unbounded (they survive without runes attached)